name: "TEST BUILD PUSH"

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    test_triggers:
        name: "test trigger"
        runs-on: ubuntu-latest
        steps:
            - name: test trigger
              run: echo "wahhoo"