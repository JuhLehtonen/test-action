on: [push, pull_request]

jobs:
  run-action:
    name: print hi
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: print hi
        uses: ./printhi